[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = ["custom_components/xray_api/proto/app", "custom_components/xray_api/proto/common"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
