name: CI

on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14.2"
      - run: python -m pip install --upgrade pip pytest ruff grpcio protobuf
      - run: python -m compileall -q custom_components/xray_api
      - run: python -m pytest tests
      - run: ruff check .
      - run: git diff --check

  runtime-minimum:
    name: Runtime dependency floor
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14.2"
      - run: python -m pip install --upgrade pip pytest grpcio==1.83.1 protobuf==4.25.3
      - run: python -m compileall -q custom_components/xray_api
      - run: python -m pytest tests/test_api.py tests/test_grpc_smoke.py
